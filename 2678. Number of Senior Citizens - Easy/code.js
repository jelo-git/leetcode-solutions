/**
 * @param {string[]} details
 * @return {number}
 */
var countSeniors = function (details) {
    let count = 0
    for (let n = 0; n < details.length; n++) {
        if (Number(details[o].charAt(11) + details[o].charAt(12)) > 60)
            count++
    }
    return count
};